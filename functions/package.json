{
  "name": "sattva-srsti-blog-functions",
  "description": "Blog reconcile + OpenAI key decrypt from admin/dummy (no env secrets)",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions:blog --project sattva-srsti"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.0"
  },
  "private": true
}
