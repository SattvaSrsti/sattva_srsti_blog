/** Blog teaser display caps — website only; does not change pipeline or recipe DB. */
window.SATTVA_TEASER = {
  ingredientShowRatio: 0.8,
  maxPreviewSteps: 4,
  showAyurveda: false,
  showPairings: true,
  showStorage: true,
};
